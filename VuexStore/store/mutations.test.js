import mutations from './mutations'

describe('mutations.js', () => {
  const {
    // exampleMutation
  } = mutations

  // it('exampleMutation', () => {
  //   // mock state
  //   const state = { }
  //   // apply mutation
  //   exampleMutation(state, new_plate)
  //   // assert result
  //   expect(state, value).toEqual(new_value)
  // })
})
